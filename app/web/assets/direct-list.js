(function(){document.addEventListener('DOMContentLoaded',function(){const chats=document.getElementById('directChats');if(!chats)return;new MutationObserver(function(){chats.classList.add('direct-chats-list');chats.querySelectorAll('.direct-chat-row').forEach(row=>{if(row.dataset.decorated)return;row.dataset.decorated='1';const text=row.textContent.trim(),parts=text.split(' — '),username=parts[0]||'',name=parts[1]||username.replace(/^@/,'');row.innerHTML='<span class="direct-chat-avatar">'+name.charAt(0).toUpperCase()+'</span><span class="direct-chat-copy"><strong class="direct-chat-name">'+name+'</strong><span class="direct-chat-username">'+username+'</span></span>'})}).observe(chats,{childList:true,subtree:true})},{once:true})})();
